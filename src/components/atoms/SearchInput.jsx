const SearchInput = () => {
  return (
    <input
      type="text"
      placeholder="Buscar..."
      className="w-full md:w-80 px-4 py-2 rounded-md bg-white text-gray-800 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-cyan-400"
    />
  );
};

export default SearchInput;
